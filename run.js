"use strict";
const player = ['nico', 1, true];
let a;
if (typeof a === 'number') {
    let b = a + 1;
}
if (typeof a === 'string') {
    let b = a.toUpperCase();
}
function hello() {
    console.log('x');
}
const c = hello();
function hello2(name) {
    if (typeof name === 'string') {
        name;
    }
    else if (typeof name === 'number') {
        name;
    }
    else {
        name;
    }
}
